fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.20  # required by FastAPI for multipart/form-data (file uploads)
h11>=0.16.0  # transitive (uvicorn/httpx); floor fixes request-smuggling CVE-2025-43859

pydantic==2.13.2
pydantic-settings==2.14.1
python-dotenv==1.2.2
requests==2.33.1

openai==2.32.0

chromadb==1.5.9
redis==7.4.0

langgraph==1.1.8
langchain==1.2.15
langchain-core>=1.3.3
langchain-openai==1.1.14
langchain-anthropic==1.4.4
langchain-groq==1.1.2
langchain-google-genai==4.2.4  # required by LLM_PROVIDER=google (config.py supports it)
langchain-community==0.4.1
langchain-chroma==1.1.0

tiktoken==0.12.0
tenacity==9.1.2  # provider retry/backoff (utils/resilience.py) — transient 429/5xx don't fail a turn
lingua-language-detector==2.1.1  # statistical EN/PT fallback for short, unaccented inputs
python-json-logger==2.0.7
fastembed==0.8.0
pillow>=12.2.0  # fastembed dependency — pinned to avoid CVEs in <12.2.0

pypdf==6.12.0  # 6.12.0 fixes xref/layout-extraction DoS CVEs (AIKIDO-2026-10937/10938)
docx2txt==0.9          # .docx text extraction (lightweight, pure-Python)
beautifulsoup4==4.14.2  # .html/.htm text extraction (uses the stdlib html.parser)
rank-bm25==0.2.2  # lexical retrieval for the optional hybrid strategy (RETRIEVAL_STRATEGY=hybrid)
